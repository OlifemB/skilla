import React, {useState} from 'react';
import {Box, Stack} from "@mui/material";
import SearchInput from "@/components/inputs/input-search";
import DropdownMenu from "@/components/dropdowns/dropdown-menu";
import {awardsData, callsData, employeesData, errorsData, sourcesData, typesData} from "@/libs/data/callsLocalData";


interface CallsFiltersProps {
    setFilterType: Function
}

const CallsFilters = ({setFilterType}: CallsFiltersProps) => {
    
    return  null
};

export default CallsFilters;